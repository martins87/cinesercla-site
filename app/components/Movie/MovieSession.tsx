import CenteredElement from "../ui/CenteredElement";
import Typography from "../Typography";

const MovieSession = () => {
  return (
    <CenteredElement>
      <Typography className="text-black">movie session here</Typography>
    </CenteredElement>
  );
};

export default MovieSession;
