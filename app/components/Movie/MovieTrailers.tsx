import CenteredElement from "../ui/CenteredElement";
import Typography from "../Typography";

const MovieTrailers = () => {
  return (
    <CenteredElement>
      <Typography className="text-black">movie trailers here</Typography>
    </CenteredElement>
  );
};

export default MovieTrailers;
